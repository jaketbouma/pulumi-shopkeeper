name: simple
runtime: nodejs
description: A simple shopkeeper example

# While developing, instruct pulumi to find the shopkeeper binary at this location.
plugins:
  providers:
    - name: shopkeeper
      path: ../../bin
